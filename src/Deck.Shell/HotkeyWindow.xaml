<Window x:Class="Deck.Shell.HotkeyWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:wv2="clr-namespace:Microsoft.Web.WebView2.Wpf;assembly=Microsoft.Web.WebView2.Wpf"
        Title="Deck shortcuts"
        Width="640" Height="560"
        MinWidth="520" MinHeight="380"
        WindowStartupLocation="CenterScreen"
        Background="#0B0D10">

    <Grid>
        <wv2:WebView2 x:Name="Web" DefaultBackgroundColor="#0B0D10" />

        <!-- Native overlay, not part of the page: WebView2 owns the keyboard while it has focus,
             so key recording has to happen in WPF. Focusing this takes Win32 focus back. -->
        <Border x:Name="KeyCatcher"
                Visibility="Collapsed"
                Focusable="True"
                Background="#E6070A0D">
            <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
                <TextBlock Text="Press a key combination"
                           Foreground="#E6EAF0" FontSize="26" FontWeight="SemiBold"
                           HorizontalAlignment="Center" />
                <TextBlock x:Name="CatcherHint"
                           Text="Esc to cancel  ·  Backspace to clear"
                           Foreground="#8B95A4" FontSize="14" Margin="0,12,0,0"
                           HorizontalAlignment="Center" />
            </StackPanel>
        </Border>
    </Grid>
</Window>
